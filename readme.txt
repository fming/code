every code can be built by two way:

1: visual studio: this way you need to create a C++ project to hold the files.

2. use MinGW: first you need to install minGW, and put fold to path: use command like:
2.1 go to that path, cd c:\users\mingfang\desktop\code
2.2 compile: g++ test_stl_1.cpp -o test.exe
3.3 run: test